# Space Raiders

Permanent browser version (GitHub Pages):

`https://chaostocode.github.io/space-raiders/`

## Auto-deploy setup

This repo now includes a GitHub Actions workflow at `.github/workflows/pages.yml`.

One-time GitHub setup:

1. Open repo `Settings`.
2. Go to `Pages`.
3. Under `Build and deployment`, set `Source` to `GitHub Actions`.

After that, every push to `main` deploys automatically.
